# music-kie-suno FAQ（よくある質問）

> 2026-09-11 時点の情報です。金額・規約は必ず公式ページの表記が優先されます。
> 他に困ったことがあれば [GitHub](https://github.com/GenKoKo/music-kie-suno-skill-lc) までお知らせください。

## 商用・権利

### Q. 生成した曲を YouTube 動画に使えますか？
A. 生成エンジンである Suno の公式ライセンス規定（https://suno.com/help/licensing）では、有償プランで「full commercial rights」（商用利用可）と明記されています（無料プランは非商用のみ）。KIE.AI の利用規約（https://kie.ai/ja/terms-of-use）は 2026-09-13 に全13条を再確認しましたが、生成楽曲の権利に関する記載は一切ありません（音楽・画像・動画の各モデル共通）。確実な回答が必要な用途では、公開前に KIE.AI サポート（https://kie.ai/vip-support）へ直接確認することをおすすめします。

## 課金・クレジット

### Q. いくらかかりますか？
A. 1リクエスト = 約12クレジット（generate music はモデル共通同額、2026-09 時点）。1リクエストで2曲できます。チャージは $5／$50／$500／$1,250 の4段階で、$500 は +5%、$1,250 は +10% のボーナスクレジットが付きます（2026-09-13 時点。詳細は公式 Billing ページの表記が優先）。

### Q. 失敗したらクレジットを消費しますか？
A. いいえ。送信失敗・生成失敗（センシティブワードを含む）は課金されません。KIE.AI 公式FAQにも「失敗したタスクは課金されません」と明記されています（2026-09-11 時点）。

### Q. クレジットに有効期限はありますか？
A. ありません。KIE.AI 公式FAQにも明記されています（2026-09-11 時点）。

### Q. 残高が足りないときは？
A. エージェントが「依頼数を減らす（残高で払える最大数を提示）」か「チャージ（https://kie.ai/ja/billing）」の2択を案内します。承認前に必ず見積もりが表示されるため、意図しない消費はありません。

### Q. 課金する前に試せますか？
A. 場合によります。公式FAQに新規ユーザーへのテスト用80無料クレジット（約6リクエスト＝12曲分）の記載がありますが、これは公式の主張であり実際の付与状況が優先します（2026-09-11 時点の確認・付与条件は変更される場合があります）。付与されていれば、登録→APIキー→設定だけで課金なしに生成できます。付与されていない場合は先にチャージが必要です。残高はエージェントが最初に確認します。

### Q. Apple Pay は使えますか？
A. はい（Chrome でも表示を確認済み）。カード番号を相手先に渡さずに支払えます。

## 生成・使い方

### Q. 生成にはどれくらい時間がかかりますか？
A. 1リクエストあたり約3〜4分（V6ファミリー、360秒曲の実測：171〜215秒、2026-09-12）。進捗は約1分ごとに表で報告されます。

### Q. 曲はどこに保存されますか？
A. `output_music_kie_suno/<日付>/` フォルダ（プロジェクト外で実行した場合は `~/Documents/output_music_kie_suno/<日付>/`）。完成後に場所が案内されます。

### Q. 保存先フォルダを変えられますか？
A. はい。エージェントに「保存先を <フォルダ> にして」と言えば設定されます（設定は環境変数 `OUTPUT_DIR_MUSIC_KIE_SUNO` に保存。解除すればデフォルトに戻ります）。

### Q. 気に入らない場合は？
A. 1リクエストで2曲できるので良い方を使ってください。両方ダメなら「もう一度」（約12クレジット）。

### Q. 気に入らない場合は？
A. 1リクエストで2曲できるので良い方を使ってください。両方ダメなら「もう一度」（約12クレジット）。

### Q. 曲名が重複することはありませんか？
A. スクリプトが過去の全生成曲名を自動チェックし、確認画面で警告します（大文字・表記ゆれも検出）。重複しそうな場合は、エージェントが別名を提案します。

### Q. 前回と同じスタイルで作り直せますか？
A. はい。全生成のスタイルと設定は `log.jsonl` に記録されています。「前回と同じスタイルで」や日時の指定でも、そのバッチのパラメータを再利用できます。

### Q. 生成結果のレポートはどこにありますか？
A. 出力フォルダ内の `report.md`（Markdown）。曲名・長さ・ファイル・消費クレジットがまとまっており、チャットにも同じ表が表示されます。

### Q. 歌詞付きの曲も作れますか？
A. はい（プランの `lyrics` に対応）。エージェントがペルソナや構成（[Verse]/[Chorus] 等）を含めた作詞を提案します。センシティブな語を含むと生成は失敗しますが、その場合も課金されません。

### Q. 大量にまとめて生成してもいいですか？
A. はい。公式の速度上限（10秒あたり20リクエスト）に合わせて、スクリプトが自動で順次送信します（余裕を持って18/10秒）。実行前の承認で総額も確認できます。

## セットアップ・トラブル

### Q. APIキーをエージェントに貼り付けてもいいですか？
A. かまいません。「設定して」と言えば環境変数の設定と残高確認まで代行します。キーは返信に表示せず、設定ファイル以外には保存しません。心配な場合は後から https://kie.ai/ja/api-key でキーを作り直せます。

### Q. Node.js が入っていないみたいです
A. エージェントにそのまま伝えてください。インストール手順（macOS / Windows）を案内します。

### Q. 401 エラー（キーが無効）が出ました
A. キーが古いか誤りです。https://kie.ai/ja/api-key で確認し、設定し直してください（エージェントへの依頼で代行もできます）。

### Q. エラーが出て生成が止まります / 途中で失敗します
A. KIE.AI 側のサーバー状況により、サービスが一時的に中断・不安定になることがあります。異常が発生した場合は、しばらく時間をおいてからもう一度お試しください（失敗したリクエストは課金されません）。続く場合は https://kie.ai/vip-support までお問い合わせください。

## KIE.AI 公式FAQから（要約・2026-09-11 時点）

> 以下は KIE.AI 公式サイト（https://kie.ai/ja）のFAQからの要約です。最新・正確な内容は必ず公式ページを優先してください。（公式）印の項目は公式FAQ由来、それ以外は本スキルの実測・運用に基づきます。

- **失敗したタスクは課金されますか？** — いいえ、失敗タスクは課金されません。（公式）
- **クレジットに有効期限はありますか？** — いいえ、ありません。利用規約第4条にも「Purchased tokens (credits) never expire」と明記されています。同条では返金サポートにも言及。（公式）
- **割引はありますか？** — 大きなチャージにはボーナスクレジットが付く場合があります（公式FAQの例：$1,250 チャージで+10%ボーナス）。（公式）
- **新規ユーザーの無料クレジット** — 公式FAQに「新規ユーザーにはテスト用に80の無料creditsが付与される」の記載あり＝公式の主張（実際の付与状況が優先・2026-09-11 時点の確認・付与条件は変更される場合があります）。付与されれば約6リクエスト＝12曲分。付与されなければ先にチャージが必要。（公式）
- **サポート窓口** — https://kie.ai/vip-support から Discord / Telegram の1対1サポートに参加できます。API統合・タスクエラー・課金質問に対応。（公式）
- **送信タスクの確認** — https://kie.ai/logs でタスク記録・ステータス・パラメータ・結果を確認できます。（公式）
- **請求書（法人向け）** — チャージ後、課金ページ（https://kie.ai/billing）から請求書を生成でき、会社情報も追加できます。（公式）
- **モデル価格の確認** — https://kie.ai/pricing でモデル別の課金単位（画像ごと・秒ごと・動画ごと・100万tokensごと等）を確認できます。（公式）
