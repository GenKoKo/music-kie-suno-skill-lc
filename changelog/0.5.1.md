# 0.5.1 — 2026-09-12 (docs: install location & model clarifications)

- Commands must run from the skill's install folder — skills.sh installs cwd-relative to `.agents/skills/music-kie-suno/`; from anywhere else pass the full path (`node <install-dir>/scripts/suno.js …`). Documented in SKILL.md Step 0 and both README install / direct-commands sections
- Cheat-sheet note: requested track length is honored on V6 (the default); V6_MINI may ignore it
- MIT LICENSE added (copyright GenKoKo); MIT badge on both READMEs
